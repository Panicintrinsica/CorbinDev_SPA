// @editorjs/marker is the one tool in use that ships no type declarations.
// Every other plugin has its own, so this file stays deliberately short — adding
// a `declare module` for a package that does ship types would shadow the real ones.
declare module '@editorjs/marker';
